name: build_bathroom
description: Plan and estimate materials and cost to build a bathroom

vars:
  city: "Fair Lawn"
  state: "NJ"
  project_desc: |
    Build a modern bathroom with 2 sinks, a walk-in shower, a freestanding tub,
    and smart mirror cabinet. Size: 10x12 ft. Located in Fair Lawn, NJ.
  requeriments_gpt4: workspace/construct/requirements_gpt4.txt
  requeriments_gpt35: workspace/construct/requirements_gpt35.txt
  requeriments_ollama: workspace/construct/requirements_ollama.txt
  materials_gpt4: workspace/construct/materials_gpt4.txt

steps:
  # - name: parse_requirements_gpt4
  #   type: ai
  #   agent: RequirementAgent
  #   namespace: construct
  #   model: ModelGpt4Turbo
  #   input:
  #     description: ${{project_desc}}
  #     location: ${{state}}, ${{city}}
  #     save_to_file: true
  #     file_name: ${requeriments_gpt4}

  # - name: parse_requirements_ollama
  #   type: ai
  #   agent: RequirementAgent
  #   model: ModelOllama
  #   namespace: construct
  #   input:
  #     description: ${{project_desc}}
  #     location: ${{state}}, ${{city}}
  #     save_to_file: true
  #     file_name: ${requeriments_ollama}

  # - name: parse_requirements_gpt35
  #   type: ai
  #   agent: RequirementAgent
  #   model: ModelGpt35Turbo
  #   namespace: construct
  #   template_name: default
  #   input:
  #     description: ${{project_desc}}
  #     location: ${{state}}, ${{city}}
  #     save_to_file: true
  #     file_name: ${requeriments_gpt35}
 
  - name: estimate_materials
    type: ai
    agent: MaterialEstimatorAgent
    namespace: construct
    model: ModelGpt4Turbo
    input:
      requirements: "$file:workspace/requirements/requirements_gpt4.txt"
      save_to_file: true
      file_name: ${materials_gpt4}

  # - name: estimate_costs
  #   type: ai
  #   agent: construct.CostEstimatorAgent
  #   model: openai.ModelGpt4Turbo
  #   prompt_template: default
  #   input:
  #     materials: "{{ estimate_materials.result }}"
  #     location: ${{state}}, ${{city}}

  # - name: generate_proposals
  #   type: ai
  #   agent: construct.ProposalGeneratorAgent
  #   model: openai.ModelGpt4Turbo
  #   prompt_template: default
  #   input:
  #     requirements: "{{ parse_requirements.requirements_yaml }}"
  #     cost_estimate: "{{ estimate_costs.result }}"
